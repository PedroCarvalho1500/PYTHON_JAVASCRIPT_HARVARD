name: Testing
on: 
  push:
    branches: [ $default-branch ]

jobs:
  test_project:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@HARVARD_COURSES
        with:
            token: ${{ secrets.MY_SECRET_HARVARD_TEST }}
      - name: Run Django unit tests
        run: |
          python --version
